#include "SimulationConstants.h"

namespace ld
{

const unsigned BASE_YEAR(2400);
const unsigned MINUTES_PER_HOUR(60);
const unsigned MINUTES_PER_DAY(1440);
const unsigned MINUTES_PER_MONTH(43200);
const unsigned MINUTES_PER_YEAR(518400);
const unsigned HOURS_PER_DAY(24);
const unsigned DAYS_PER_MONTH(30);
const unsigned MONTHS_PER_YEAR(12);
const float LENGTH_OF_GAME_MINUTE(4.f);

}
